import styles from "./nodos.module.css";

export default function Nodos() {
    return (
        <section id="nodes" className={styles.nodos}>
            <div className={styles.content}>
                <span>Nodos</span>
            </div>
        </section>
    )
}